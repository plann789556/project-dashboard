const d=dashboardData,p=d.projects;const latest=x=>x.progress.at(-1);const done=p.filter(x=>x.status==='แล้วเสร็จ').length,onplan=p.filter(x=>x.status==='เป็นไปตามแผน').length,avg=p.reduce((s,x)=>s+latest(x),0)/p.length;total.textContent=p.length;document.querySelector('#done').textContent=done;document.querySelector('#onplan').textContent=onplan;document.querySelector('#avg').textContent=avg.toFixed(1);new Chart(document.querySelector('#statusChart'),{type:'doughnut',data:{labels:['แล้วเสร็จ','เป็นไปตามแผน'],datasets:[{data:[done,onplan],backgroundColor:['#28735a','#c6a15b'],borderWidth:0}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'bottom'}}}});const avgs=d.months.map((_,i)=>p.reduce((s,x)=>s+x.progress[i],0)/p.length);new Chart(document.querySelector('#trendChart'),{type:'line',data:{labels:d.months,datasets:[{label:'ความก้าวหน้าเฉลี่ย (%)',data:avgs,borderColor:'#28735a',backgroundColor:'#28735a22',fill:true,tension:.3,pointRadius:3}]},options:{responsive:true,maintainAspectRatio:false,scales:{y:{min:0,max:100}}}});function render(){const q=search.value.toLowerCase(),f=filter.value;projects.innerHTML=p.filter(x=>(!q||x.name.toLowerCase().includes(q))&&(!f||x.status===f)).map(x=>`<div class="project"><div><div class="name">${x.name}</div><div class="bar"><i style="width:${latest(x)}%"></i></div></div><span class="badge ${x.status==='เป็นไปตามแผน'?'plan':''}">${x.status}</span><div class="pct">${latest(x)}%</div></div>`).join('')}search.oninput=filter.onchange=render;render();
